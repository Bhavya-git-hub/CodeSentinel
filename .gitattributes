# Normalise to LF in the repository. CI and the container images are Linux; a CRLF
# shell script or Dockerfile committed from Windows fails there in ways that are
# tedious to diagnose.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
