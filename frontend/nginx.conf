server {
  listen 80;
  root /usr/share/nginx/html;

  # Single-page app: unknown paths must return index.html, or refreshing on
  # /scans/<id> would 404 against a route that only exists in the browser.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
