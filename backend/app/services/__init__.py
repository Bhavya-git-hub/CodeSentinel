"""Domain services.

Each subpackage is introduced in the build phase that needs it:
sandbox (phase 2), ingestion + analyzers (phase 3), mining + scoring (phase 4),
graph (phase 6), reporting (phase 7), prediction (phase 8).
"""
